/**
 * Конфигурация игры Blast по mechanics.md.
 * Группа от 2 тайлов; 5–9 — бустер (ракета/бомба), 10+ — мощный бустер + бонусные ходы.
 */
export const GameConfig = {
    /** Ширина поля (колонок) */
    COLS: 8,
    /** Высота поля (рядов) */
    ROWS: 8,
    /** Количество обычных цветов тайлов (индексы 0 .. COLORS-1) */
    COLORS: 5,
    /** Цель по очкам для победы */
    TARGET_SCORE: 1000,
    /** Максимум ходов */
    MAX_MOVES: 20,

    /** Минимум тайлов в группе для сжигания (одиночный тап — не тратит ход, подсказка пульс) */
    MIN_GROUP_SIZE: 2,
    /** 5–9 тайлов: создаётся бустер (ракета или бомба) */
    BOOSTER_GROUP_MIN: 5,
    BOOSTER_GROUP_MAX: 9,
    /** 10+ тайлов: мощный бустер (бомба-макс) + бонусные ходы */
    POWER_BOOSTER_GROUP_MIN: 10,
    /** Бонусных ходов за группу 10+ */
    BONUS_MOVES_POWER: 2,

    /** Максимум авто-перемешиваний при отсутствии ходов (далее — проигрыш). */
    MAX_SHUFFLES: 3,

    /** Бустеры из UI: стартовое количество использований. */
    DEFAULT_BOMB_BOOSTERS: 5,
    DEFAULT_TELEPORT_BOOSTERS: 3,

    /** Сколько случайных бомб (5–8) поставить на поле при старте игры (0 = только обычные тайлы). */
    INITIAL_BOMB_COUNT: 0,

    /** Типы тайлов: 0–4 = цвет, 5–8 = спецтайлы, 9 = очистить всё, 10 = телепорт (обмен двух) */
    TILE_ROCKET_H: 5,
    TILE_ROCKET_V: 6,
    TILE_BOMB: 7,
    TILE_BOMB_MAX: 8,
    TILE_CLEAR_ALL: 9,
    TILE_TELEPORT: 10,

    /** Радиус бомбы (1 = 3x3) */
    BOMB_RADIUS: 1,
    /** Радиус бомбы-макс (2 = 5x5) */
    BOMB_MAX_RADIUS: 2,

    /** Очки за сжигание группы обычных тайлов */
    scoreForGroup(size: number): number {
        return size * 10;
    },
    /** Очки за один сожжённый тайл (спецэффекты) */
    scorePerTile: 10,
} as const;

export type BoardValue = number; // -1 пусто, 0..4 цвет, 5..10 спецтайл
